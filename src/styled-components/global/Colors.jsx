import { createGlobalStyle } from 'styled-components';


const Colors = createGlobalStyle`
:root {
    --primaryDark: #000000;
    --primaryLight: #ffffff;
    --primaryAccent: #800080;
}
`;

export default Colors;