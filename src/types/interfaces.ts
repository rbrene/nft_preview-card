export type CardPropsTypes = {
    data: {
        images: {
            preview: string;
            avatar: string
        };
        header: string;
        description: string;
        currency: string;
        timeLimit: number;
        designer: string;
    }
}